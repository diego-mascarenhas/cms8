<div class="dashboard-calendar-empty d-flex flex-column align-items-center justify-content-center text-center px-3 py-4">
    <i class="ti {{ $emptyIcon }} text-muted ti-3x mb-3"></i>
    <h6 class="mb-1">{{ $emptyTitle }}</h6>
    <p class="text-muted mb-0 small">{{ $emptyMessage }}</p>
</div>
